---
title: "Страница не найдена (404)"
layout: layouts/base.njk
permalink: /404.html
eleventyExcludeFromCollections: true
---

<style>
	.error-page-container {
		max-width: 700px;
		margin: 80px auto;
		text-align: center;
		padding: 40px 20px;
	}
	.error-code {
		font-size: 8rem;
		font-weight: 900;
		background: linear-gradient(135deg, var(--primary), var(--neon));
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		margin: 0;
		line-height: 1;
	}
	.error-title {
		font-size: 2rem;
		color: var(--text-light);
		margin: 20px 0 15px;
	}
	.error-description {
		font-size: 1.1rem;
		color: var(--text-gray);
		line-height: 1.6;
		margin-bottom: 40px;
	}
	.error-actions {
		display: flex;
		gap: 15px;
		justify-content: center;
		flex-wrap: wrap;
	}
	.error-actions .cta-button {
		display: inline-block;
		padding: 15px 35px;
		background: linear-gradient(135deg, var(--primary), var(--secondary));
		color: var(--text-light);
		text-decoration: none;
		border-radius: 50px;
		font-weight: 600;
		font-size: 1rem;
		transition: transform 0.2s, box-shadow 0.2s;
		box-shadow: 0 4px 15px var(--primary-glow);
	}
	.error-actions .cta-button:hover {
		transform: translateY(-2px);
		box-shadow: 0 6px 20px var(--primary-glow);
	}
	.error-actions .secondary-link {
		display: inline-block;
		padding: 15px 35px;
		background: var(--bg-surface);
		color: var(--text-light);
		text-decoration: none;
		border-radius: 50px;
		font-weight: 600;
		font-size: 1rem;
		border: 2px solid var(--border);
		transition: all 0.2s;
	}
	.error-actions .secondary-link:hover {
		border-color: var(--primary);
		color: var(--primary);
		transform: translateY(-2px);
	}
	.error-search {
		margin-top: 50px;
		padding-top: 40px;
		border-top: 1px solid var(--border);
	}
	.error-search h3 {
		font-size: 1.3rem;
		color: var(--text-light);
		margin-bottom: 20px;
	}
	.error-search-form {
		display: flex;
		gap: 10px;
		max-width: 500px;
		margin: 0 auto;
	}
	.error-search-form input {
		flex: 1;
		padding: 15px 20px;
		border: 2px solid var(--border);
		border-radius: 12px;
		background: var(--bg-surface);
		color: var(--text-light);
		font-size: 1rem;
		outline: none;
		transition: border-color 0.2s;
	}
	.error-search-form input:focus {
		border-color: var(--primary);
	}
	.error-search-form button {
		padding: 15px 30px;
		background: var(--primary);
		color: var(--text-light);
		border: none;
		border-radius: 12px;
		font-size: 1rem;
		font-weight: 600;
		cursor: pointer;
		transition: background 0.2s;
	}
	.error-search-form button:hover {
		background: var(--secondary);
	}
	.error-popular {
		margin-top: 40px;
		text-align: left;
	}
	.error-popular h3 {
		font-size: 1.2rem;
		color: var(--text-light);
		margin-bottom: 15px;
	}
	.error-popular ul {
		list-style: none;
		padding: 0;
		margin: 0;
	}
	.error-popular li {
		padding: 10px 0;
		border-bottom: 1px solid var(--border);
	}
	.error-popular li:last-child {
		border-bottom: none;
	}
	.error-popular a {
		color: var(--text-gray);
		text-decoration: none;
		transition: color 0.2s;
	}
	.error-popular a:hover {
		color: var(--primary);
	}
</style>

<div class="error-page-container">
	<h1 class="error-code">404</h1>
	<h2 class="error-title">Сигнал потерян в космосе...</h2>
	<p class="error-description">
		Похоже, вы сбились с курса. Страница, которую вы ищете, могла быть перемещена, удалена или никогда не существовала.
	</p>
	<div class="error-actions">
		<a href="/blog/" class="cta-button">Вернуться в блог</a>
		<a href="/" class="secondary-link">На sethubble.ru</a>
	</div>

	<div class="error-search">
		<h3>🔍 Или найдите то, что искали:</h3>
		<form class="error-search-form" action="/search/" method="get" onsubmit="window.location.href='/search/?q='+this.q.value;return false;">
			<input type="text" name="q" placeholder="Поиск по статьям..." required>
			<button type="submit">Найти</button>
		</form>
	</div>

	<div class="error-popular">
		<h3>📈 Популярные статьи:</h3>
		<ul>
			{%- for post in collections.posts | head(5) %}
			<li><a href="{{ post.url }}">→ {{ post.data.title }}</a></li>
			{%- endfor %}
		</ul>
	</div>
</div>
